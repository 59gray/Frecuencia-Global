---
import BaseLayout from './BaseLayout.astro';
import EditorialPageShell from '../components/EditorialPageShell.astro';
import PillarPill from '../components/PillarPill.astro';
import FrequencyLine from '../components/FrequencyLine.astro';

interface PlatformLinks {
  spotify?: string;
  apple?: string;
  youtube?: string;
}

interface Props {
  title: string;
  description: string;
  publishDate: Date;
  pilar: 'p1' | 'p2' | 'p3' | 'p4';
  season: number;
  episodeNumber: number;
  duration: string;
  audioUrl: string;
  coverImage: string;
  youtubeVideoId: string;
  videoMode: 'editorial-visualizer' | 'on-camera' | 'hybrid';
  explicit: boolean;
  platformLinks?: PlatformLinks;
}

const {
  title,
  description,
  publishDate,
  pilar,
  season,
  episodeNumber,
  duration,
  audioUrl,
  coverImage,
  youtubeVideoId,
  videoMode,
  explicit,
  platformLinks = {},
} = Astro.props;

const dateFormatted = publishDate.toLocaleDateString('es-MX', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

const platformButtons = [
  platformLinks.spotify ? { label: 'Spotify', href: platformLinks.spotify } : null,
  platformLinks.apple ? { label: 'Apple Podcasts', href: platformLinks.apple } : null,
  platformLinks.youtube ? { label: 'YouTube', href: platformLinks.youtube } : null,
].filter(Boolean) as { label: string; href: string }[];

const videoModeLabel = videoMode === 'editorial-visualizer' ? 'Editorial audiovisual 16:9' : videoMode;
---

<BaseLayout
  title={title}
  description={description}
  type="article"
  pilar={pilar}
  image={coverImage}
>
  <EditorialPageShell density="standard" texture="radar">
  <article class="mx-auto max-w-[1500px] px-fg-md py-fg-2xl lg:px-fg-xl">
    <header class="grid gap-10 border-b border-white/10 pb-fg-xl lg:grid-cols-[minmax(0,1.2fr)_380px]">
      <div>
        <div class="flex flex-wrap items-center gap-3">
          <PillarPill pilar={pilar} size="md" />
          <span class="font-data text-[11px] uppercase tracking-[0.28em] text-fg-gray">
            temporada {season} / episodio {String(episodeNumber).padStart(2, '0')}
          </span>
        </div>

        <h1 class="mt-5 max-w-[20ch] font-display text-4xl sm:text-5xl tracking-wide leading-tight text-fg-white">
          {title}
        </h1>

        <p class="mt-5 max-w-3xl text-lg leading-relaxed text-fg-gray">
          {description}
        </p>

        <div class="mt-8 flex flex-wrap gap-3">
          <a
            href={`https://www.youtube.com/watch?v=${youtubeVideoId}`}
            class="inline-flex items-center justify-center border border-fg-cyan bg-fg-cyan px-5 py-3 font-data text-xs uppercase tracking-[0.26em] text-fg-black transition-opacity duration-300 hover:opacity-90"
          >
            ver videopodcast
          </a>
          <a
            href={audioUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center justify-center border border-white/15 px-5 py-3 font-data text-xs uppercase tracking-[0.26em] text-fg-white transition-colors duration-300 hover:border-white/35 hover:bg-white/5"
          >
            abrir audio
          </a>
          {platformButtons.map((platform) => (
            <a
              href={platform.href}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center justify-center border border-white/15 px-5 py-3 font-data text-xs uppercase tracking-[0.26em] text-fg-gray transition-colors duration-300 hover:border-fg-cyan hover:text-fg-cyan"
            >
              {platform.label}
            </a>
          ))}
        </div>
      </div>

      <aside class="fg-tactical-panel fg-corner-cut overflow-hidden">
        <img src={coverImage} alt={title} class="aspect-video w-full object-cover" />
        <div class="space-y-5 px-6 py-6">
          <dl class="grid grid-cols-2 gap-4 font-data text-[10px] uppercase tracking-[0.24em] text-fg-gray">
            <div>
              <dt>publicado</dt>
              <dd class="mt-1 text-fg-white">{dateFormatted}</dd>
            </div>
            <div>
              <dt>duración</dt>
              <dd class="mt-1 text-fg-white">{duration}</dd>
            </div>
            <div>
              <dt>formato</dt>
              <dd class="mt-1 text-fg-white">{videoModeLabel}</dd>
            </div>
            <div>
              <dt>explícito</dt>
              <dd class="mt-1 text-fg-white">{explicit ? 'Sí' : 'No'}</dd>
            </div>
          </dl>

          <div class="fg-corner-cut border border-white/12 bg-fg-black/50 px-4 py-4">
            <p class="font-data text-[10px] uppercase tracking-[0.24em] text-fg-gray">version visual</p>
            <p class="mt-3 text-sm leading-relaxed text-fg-gray">
              Edicion 16:9 lista para YouTube: narrativa lineal, transiciones sobrias y paquete visual coherente con el episodio.
            </p>
          </div>
        </div>
      </aside>
    </header>

    <section class="mt-fg-xl grid gap-10 lg:grid-cols-[minmax(0,1.15fr)_360px]">
      <div>
        <div class="fg-corner-cut aspect-video overflow-hidden border border-white/12 bg-black/40 shadow-[0_0_48px_rgba(0,229,255,0.04)]">
          <iframe
            class="h-full w-full"
            src={`https://www.youtube.com/embed/${youtubeVideoId}`}
            title={`${title} - Videopodcast`}
            loading="lazy"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
            allowfullscreen
          ></iframe>
        </div>

        <div class="fg-tactical-panel fg-corner-cut mt-fg-lg overflow-hidden px-5 py-5">
          <div class="flex flex-wrap items-center justify-between gap-3 border-b border-white/10 pb-4">
            <div>
              <p class="font-data text-[10px] uppercase tracking-[0.24em] text-fg-gray">audio completo</p>
              <p class="mt-1 text-sm text-fg-gray">Disponible en web y plataformas de podcast</p>
            </div>
            <span class="font-data text-[10px] uppercase tracking-[0.24em] text-fg-cyan">{duration}</span>
          </div>
          <audio class="mt-5 w-full" controls preload="none" src={audioUrl}>
            Tu navegador no soporta el reproductor de audio.
          </audio>
        </div>

        <div class="mt-fg-xl">
          <p class="font-data text-[11px] uppercase tracking-[0.3em] text-fg-gray">show notes</p>
          <div class="mt-4">
            <FrequencyLine />
          </div>
          <div class="prose mt-8">
            <slot />
          </div>
        </div>
      </div>

      <aside class="space-y-6">
        <div class="fg-tactical-panel fg-corner-cut px-6 py-6">
          <p class="font-data text-[11px] uppercase tracking-[0.3em] text-fg-gray">edicion del episodio</p>
          <ul class="mt-5 space-y-3 text-sm leading-relaxed text-fg-gray">
            <li>master de audio validado para distribucion</li>
            <li>videopodcast largo con sincronizacion final</li>
            <li>capitulos, resumen ejecutivo y enlaces de referencia</li>
            <li>cortes derivados para Shorts, Reels y TikTok</li>
          </ul>
        </div>

        <div class="fg-tactical-panel fg-corner-cut px-6 py-6">
          <p class="font-data text-[11px] uppercase tracking-[0.3em] text-fg-gray">donde escucharlo</p>
          <ul class="mt-5 space-y-3 text-sm leading-relaxed text-fg-gray">
            <li>
              <a href="/podcast" class="text-fg-cyan hover:text-fg-white">archivo del podcast</a>
            </li>
            <li>
              <a href="/podcast/rss.xml" class="text-fg-cyan hover:text-fg-white">canal RSS del podcast</a>
            </li>
            <li>
              <a href="/contenido" class="text-fg-cyan hover:text-fg-white">archivo editorial completo</a>
            </li>
          </ul>
        </div>
      </aside>
    </section>
  </article>
  </EditorialPageShell>
</BaseLayout>
