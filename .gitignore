# === Local secrets (nunca commitear) ===
.env.local
*.local.env
secrets.local.ps1
.env.local.*

# === n8n ===
08_n8n/.env
08_n8n/.env.cloud
08_n8n/.env.telegram
08_n8n/n8n_data/

# === Secrets ===
scripts/client_secret*.json
scripts/token.json
*.secret
*.key

# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === Local browser profiles / automation traces ===
.chrome-*/
.playwright-mcp/
chrome-sessions/

# Scripts: temp clones, debug screenshots, Chrome test profiles (never commit)
scripts/tmp_*/
scripts/*_diag/

scripts/diag*.png
scripts/debug*.png
scripts/fail*.png
scripts/font_*.png
scripts/fonts_*.png
scripts/noresult_*.png
scripts/result_*.png
scripts/subtitle_*.png
scripts/test_*.png
website/dist/

# === Editor ===
.vscode/settings.json
*.swp
*~

# === Node ===
node_modules/

# === Python ===
__pycache__/
*.pyc
.env

# === Archive / temp ===
_archivo/
scripts/_archive/
tmp/
output/
logs/

# Ops: logs de ejecución (alto churn; JSON de checkpoint puede seguir trackeado)
07_Operaciones/logs/*.log

# Capturas sueltas en raíz de scripts
scripts/tiktok_before_verify.png
.sixth/
.venv/

# === D15 — Ruido local (tooling, IDE embebido, backups de reparación, artefactos de ejecución)
# No incluye fuente del producto ni 07_Operaciones/*.md|csv (siguen versionables según política ops).
.claude/
.cursor/
.vs/
.pytest_cache/

# Snapshot de emergencia tras corrupción de índice Git (solo disco local).
.git_corrupto_backup_*/

# Copias de trabajo y bundles de rescate (no fuente).
**/backups/
_repair_backups/

# Perfiles de navegador / seguridad local (no versionar).
BrowserProfiles/
Security/

# Logs de desarrollo y ejecución (no sustituye logs/ ya ignorado arriba).
*.log

# === D16 — Cola residual (post D15): incidentes, artefactos de job, previews locales
# Evita staging accidental de bundles Git y escaneos de secretos (solo rutas; no se lee contenido).
incident_response/

# Copias retiradas y manifests de backup puntual en ops.
07_Operaciones/_retired_artifacts/
07_Operaciones/D__ARTIFACT_BACKUP_manifest_*.json
07_Operaciones/FG_PROD_SNAPSHOT_AFTER_DEPLOY_*.html

# Salidas de jobs en raíz (no editorial).
/D07_outputs.json
/D07_job_ids.json
/asks_fg_*.csv

# Capturas de preview fuera de public/ (ruido local).
website/bass-borders-preview.png
website/bass-borders-preview-full.png
